.navbar.navbar-default.navbar-fixed-top{:role => "navigation"}
  .container
    .navbar-header
      %button.navbar-toggle{"data-target" => ".navbar-collapse", "data-toggle" => "collapse", :type => "button"}
        %span.sr-only Toggle navigation
        %span.icon-bar
        %span.icon-bar
        %span.icon-bar
      %a.navbar-brand{:href => root_path}
        = image_tag(@logo.image.url(:medium))
    .collapse.navbar-collapse
      %ul.nav.navbar-nav
        - @categories.each do |category|
          - if category.children.empty?
            %li= link_to category.name.upcase, category_path(category)
          - else
            %li.dropdown-hover
              %a.dropdown-toggle{"data-toggle" => "dropdown"}
                = category.name.upcase
                %span.caret
                %ul.dropdown-menu
                  - category.children.each do |first_tier|
                    - if first_tier.children.empty?
                      %li= link_to first_tier.name.upcase, category_path(first_tier)
                    - else
                      %li.dropdown-submenu.dropdown-menu-right
                        %a
                          = first_tier.name.upcase
                          %i.fa.fa-caret-right
                        %ul.dropdown-menu.dropdown-hover
                          - first_tier.children.each do |second_tier|
                            %li= link_to second_tier.name.upcase, category_path(second_tier)
        %li= link_to "CHINA DESK", china_desk_path
        %li= link_to "CONTACT", contact_path
